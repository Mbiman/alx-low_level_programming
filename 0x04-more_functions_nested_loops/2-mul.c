#include "main.h"
/**
 * mul - is a function that multiplies two numbers
 * @a: a is a parameter in the function mul
 * @b: b is a parameter in the function mul
 *
 * Return: integer
 */
int mul(int a, int b)
{
	int f;
	f = a * b;
	return (f);
}
